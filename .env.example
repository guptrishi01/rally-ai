# Copy this file to .env (gitignored) and fill in a real key.
# Used by scripts/client.py to construct the real Anthropic client for
# entry points that intentionally spend real API money (pipeline.suggest(),
# ai.pipeline.AICoachPipeline.generate()). Library code never reads this
# directly - the client is always constructed here and injected in.
ANTHROPIC_API_KEY=